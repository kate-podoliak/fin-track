import styles from "../../assets/styles/header.module.scss"

export const Header = () => {
    return (
        <div className={styles.header}>
            <div>
                <span>Header</span>
            </div>
        </div>
    );
};